name: RegistrationPlugin
version: '${project.version}'
main: network.vonix.registrationplugin.RegistrationPlugin
api-version: '1.20'
commands:
  register:
    description: Register with your email and password.
    usage: /register <email> <password>